# قايمة المؤجّلات — مشروع Talia 360

الهدف من الملف ده: أي حاجة قررنا نأجّلها بوعي (مش نسيناها) بتتسجّل هنا، مع سبب التأجيل والخطوات المطلوبة لما نيجي نكمّلها. لو دخلت على المشروع بعد فترة، ابدأ من هنا.

---

## 1. الأمان — Row Level Security (RLS)

**الحالة الحالية:** مفيش RLS على أي جدول في Supabase. أي حد معاه الـ anon key (اللي هو موجود في كود الفرونت إند) يقدر يقرا/يعدّل أي بيانات في القاعدة كلها، من غير تسجيل دخول.

**ليه اتأجل:** التطبيق لسه في مرحلة الديمو تحت إشراف مباشر، ومش متاح لعملاء حقيقيين ببيانات طلاب حقيقية.

**⚠️ شرط لازم يتحقق قبل أي استخدام حقيقي (حتى لو مدرسة واحدة تجريبية ببيانات حقيقية):** لازم تتعمل RLS policies على كل جدول قبل ما أي بيانات طالب حقيقية تتحط في النظام.

---

## 2. نظام تسجيل الدخول الحقيقي

**الحالة الحالية:** الباسورد متخزّن كنص عادي (plaintext) في عمود `password` بجدول `users`. مفيش تشفير، مفيش "نسيت الباسورد"، مفيش Supabase Auth حقيقي. الـ login الحالي بيقارن الإيميل والباسورد مباشرة.

**ليه اتأجل:** أبسط للتطوير السريع في مرحلة الديمو، وربط RLS الحقيقي محتاج يتعمل مع نظام الـ Auth الحقيقي مع بعض (مترابطين).

**لما نرجعله:** هيتعمل مع بند رقم 1 (RLS) في نفس المرحلة، لأن الاتنين مترابطين تقنيًا (RLS بيعتمد على `auth.uid()` من Supabase Auth).

---

## 3. معمارية المشروع: Multi-tenant SaaS

**القرار المتفق عليه:** المنصة هتبقى **multi-tenant** (منصة واحدة مشتركة لكل المدارس، زي Canvas وBlackboard)، مش نسخة منفصلة لكل مدرسة.

**الحالة الحالية:** القاعدة كلها مبنية لمدرسة واحدة بس — مفيش عمود `school_id` في أي جدول.

**اللي محتاجين نعمله لما نيجي نبنيه:**
- إضافة `school_id` لكل جدول تقريبًا
- جدول `schools` جديد
- كل الكويريز (كل دوال `services/supabaseData.ts`) لازم تتفلتر بالـ `school_id`
- ده بيترابط مباشرة مع بند RLS (رقم 1) — العزل بين المدارس هيتفرض عن طريق RLS policies مبنية على `school_id`
- تصميم onboarding لمدرسة جديدة تسجّل نفسها

**ليه اتأجل:** التركيز حاليًا على تكملة الفيتشرز الوظيفية وربط Talia Learn، ومفيش تعارض بين الشغل ده والتأجيل.

---

## 4. أجزاء لسه شكلية/وهمية بالكامل (مش مؤجّلة بقرار، بس لسه مبنيتش)

| الموديول | الحالة |
|---|---|
| لوحة التحكم الرئيسية (Dashboard) | بيانات وهمية |
| الشؤون المالية | بيانات وهمية |
| التحليلات (Analytics) | بيانات وهمية |
| تاب أولياء الأمور (Students) | وهمي |
| التنبيهات/الإشعارات | البنية موجودة، مش حقيقية |
| رفع ملفات حقيقي (Storage) | مفيش Supabase Storage متجهّز — أي "رفع" حاليًا شكلي |
| **Talia Learn (تطبيق الطالب/LMS)** | تسجيل الدخول الحقيقي اشتغل (نفس حسابات Talia 360)، باقي بيانات الدرجات/الحضور/المحتوى لسه وهمية |

---

## 5. حساب ولي الأمر — مفيش واجهة لإنشائه لسه

نظام تسجيل الدخول في Talia Learn بقى بيتحقق من حساب ولي الأمر الحقيقي (`parents` + `parent_students`)، **لكن مفيش شاشة "إضافة ولي أمر" في Talia 360 لسه** (تاب أولياء الأمور لسه وهمي، بند رقم 4). يعني عمليًا: لحد ما نبني الشاشة دي، أي حساب ولي أمر للتجربة لازم يتضاف يدويًا من Supabase (Table Editor) مباشرة.

---

## آخر تحديث
اتعمل تسجيل الدخول الحقيقي (معلم/طالب/ولي أمر) في Talia Learn، بنفس حسابات Talia 360، وجدول `parent_students` الجديد اللي بيربط ولي الأمر بأبنائه.
